'use client';
import React from 'react'

const ClientButtonComponent = () => {
    return (
        <>
            <button onClick={() => alert("It is Client BUTTON :)")}>Here We go</button>
        </>
    )
};

export default ClientButtonComponent
